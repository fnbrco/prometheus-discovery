{
  "name": "prometheus-discovery",
  "version": "1.0.0",
  "description": "Service to discover active web servers for prometheus scraping",
  "main": "entry.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "private": true,
  "repository": {
    "type": "git",
    "url": "git+https://github.com/fnbrco/prometheus-discovery.git"
  },
  "dependencies": {
    "do-wrapper": "^4.4.1",
    "restify": "^8.5.1",
    "rfr": "^1.2.3"
  }
}
